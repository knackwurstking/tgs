# NOTE: This is just an example
define:
    - &AliceChatID <number>

# `[]tgs.BotCommandScope`
register:
    - type: default
    - type: chat
      chat_id: *AliceChatID

# `extension.Targets`
targets:
    all: true
